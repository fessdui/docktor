<IfModule mod_rewrite.c>
    Options -MultiViews +FollowSymlinks

    RewriteEngine On
    #RewriteBase /path/to/app
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
#    RewriteRule ^ index.php public/$1 [L]
</IfModule>